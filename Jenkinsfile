pipeline {
    agent any


    stages {
        stage('Checkout') {
            steps {
                script{
                sh 'echo "Hello"'
                sh 'ls -ltr'
                sh 'pwd'
                }

            }
        }
    }
}
